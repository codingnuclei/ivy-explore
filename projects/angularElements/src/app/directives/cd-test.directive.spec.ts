import { CdTestDirective } from './cd-test.directive';

describe('CdTestDirective', () => {
  it('should create an instance', () => {
    const directive = new CdTestDirective();
    expect(directive).toBeTruthy();
  });
});
